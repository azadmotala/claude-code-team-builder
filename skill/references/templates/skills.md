# Skill File Templates

Use these as starting points for the skills generated by software-project-team-builder.
Always replace placeholders with project-specific commands, paths, and context.

---

## Execution Skills (always included)

### /run

```markdown
---
name: run
description: Autonomous execution loop for [Project Name]. Plans tasks, assigns agents, validates results, self-heals on failure. Use `/run --plan` to generate a plan without executing. Use `/run` to start or resume execution. Use `/run --task [id]` to execute a single task.
---

# Run

Autonomous execution loop for [Project Name].

## Usage
- `/run --plan` — decompose project into milestones and tasks, show the plan, don't execute
- `/run` — start or resume execution from current state
- `/run --task m1-t3` — execute a single specific task
- `/run --milestone m2` — execute all tasks in milestone 2

## Execution Loop

### 0. Reconcile State (always first)
- Read `.claude/workspace/orchestrator_state.json` if it exists
- Read `.claude/tasks.json`
- Scan `.claude/workspace/` for result files
- If a task is marked `done` in tasks.json but has no result file → mark as `review`
- If a result file exists but tasks.json shows `pending` → validate and mark `done` if criteria pass
- Write reconciled state to `orchestrator_state.json`
- This prevents drift from interrupted sessions or manual edits

### 1. Generate dashboard
- Copy `references/templates/dashboard.html` to `.claude/workspace/dashboard.html`
- Do NOT regenerate from scratch — always use the fixed template
- The dashboard reads `tasks.json` and `progress.log` dynamically via fetch

### 2. Pick next ready task
- Find the highest-priority task with status `pending` and all dependencies in status `done`
- If no tasks are ready, check for blocked tasks and report why

### 3. Assign to agent
- Use CLAUDE.md agent routing to select the correct agent
- Invoke the agent with the task definition and acceptance criteria

### 4. Validate result
- Read the agent's result file from `.claude/workspace/[task-id].result.md`
- Check each acceptance criterion — pass or fail
- If all pass → mark task `done`, log to `progress.log`
- If any fail → enter self-healing pipeline

### 5. Self-healing pipeline (on failure)
Read retry policy from `settings.json`. Classify the failure first:

**Simple failure** (syntax error, missing import, typo, wrong path):
- Retry once with the same agent plus a hint describing the error
- Do NOT invoke the problem-solver — the round-trip costs more than a retry
- If retry also fails → escalate to problem-solver

**Structural failure** (wrong decomposition, missing dependency, vague criteria):
- Invoke the problem-solver immediately

**Problem-solver pipeline** (when invoked):
1. **Refine** (attempts 1–2): problem-solver rewrites the task
2. **Split** (attempt 3): problem-solver decomposes into subtasks
3. **Reassign** (attempt 4): try a different qualified agent
4. **Escalate or skip**: based on `settings.json` autonomy mode

### 6. Milestone boundary
- When all tasks in a milestone are `done`:
  - If `auto_advance_milestones` is true → validate milestone outputs and advance
  - If false → pause and ask for human review

### 7. Completion check
- After each milestone, check project completion criteria from CLAUDE.md
- If all criteria met → report project complete
- If not → continue to next milestone

## State Files
- `tasks.json` — task definitions, status, dependencies, assignment
- `workspace/orchestrator_state.json` — persistent orchestrator memory
- `workspace/progress.log` — human-readable task transition log
- `workspace/dashboard.html` — visual progress tracker (auto-generated at execution start)
- `workspace/[task-id].result.md` — agent result files
- `workspace/[task-id]-repair.md` — problem-solver repair reports
```

### /status

```markdown
---
name: status
description: Show current project state for [Project Name]. Reports what's done, in progress, blocked, and next. Run any time to see where the project stands.
---

# Status

Reports project state for [Project Name].

## Workflow

1. **Read state** — load `tasks.json` and `orchestrator_state.json`
2. **Categorize tasks**:
   - ✅ Done — completed and validated
   - 🔄 In progress — currently assigned
   - 🚫 Blocked — dependencies not met
   - ⏳ Pending — ready but not started
   - 🔴 Failed — exceeded retry limit
3. **Show milestone progress** — percentage complete per milestone
4. **Show recent activity** — last 5 entries from `progress.log`
5. **Show next action** — what the orchestrator will do next
6. **Show failures** — any tasks that failed with attempt count and reason

## Output format
Print to console. Group by milestone. Show counts at the top: `Done: X | In Progress: X | Blocked: X | Pending: X | Failed: X`
```

### /dashboard

```markdown
---
name: dashboard
description: Generate the visual dashboard for [Project Name]. Copies the fixed dashboard template to the workspace. The dashboard reads tasks.json and progress.log from disk and auto-refreshes every 5 seconds.
---

# Dashboard

Copies the dashboard template to `.claude/workspace/dashboard.html`.

## Workflow

1. **Copy the template** — copy `references/templates/dashboard.html` to `.claude/workspace/dashboard.html`
2. **Do NOT regenerate the HTML from scratch** — always use the fixed template. This ensures consistent styling and behavior across runs.
3. **Instruct user**: "Open in a browser. Serve with `python -m http.server 8000` from `.claude/workspace/` for auto-refresh."

## How the dashboard works
- Reads `tasks.json` and `progress.log` via fetch (relative paths)
- Auto-refreshes every 5 seconds
- Shows: status counts (done, in progress, pending, blocked, failed), overall progress bar, milestone progress bars, task list with status badges, activity timeline from progress.log
- No generation needed — the same HTML file works for any project because it reads task data dynamically

## Important
- The dashboard must be served from `.claude/workspace/` so it can find `tasks.json` and `progress.log`
- If `tasks.json` is in `.claude/` (one level up), the dashboard tries `../tasks.json` as a fallback
- The template lives in `references/templates/dashboard.html` — do not modify it per project
```

---

## Workflow Skills

### /deploy

```markdown
---
name: deploy
description: Deploy [Project Name] to staging or production. Use when you're ready to ship a feature or want to test in a live environment. Handles build, test gate, and deployment to [hosting provider].
---

# Deploy

Deploys [Project Name] to the target environment.

## Usage
- `/deploy` — deploy to staging (default)
- `/deploy --prod` — deploy to production (confirms before proceeding)

## Workflow

1. **Pre-flight check** — verify no uncommitted changes, run `[test command]`
2. **Build** — run `[build command]` and confirm it succeeds
3. **Deploy to staging** — run `[staging deploy command]`
4. **Smoke test** — verify `[key URL or health endpoint]` responds correctly
5. **If --prod**: confirm with user, then run `[production deploy command]`

## Project specifics
- Staging URL: [staging URL or "check .env.staging"]
- Production URL: [production URL]
- Deploy command: `[e.g., vercel --prod, railway up, npm run deploy]`
- Rollback: `[e.g., vercel rollback, railway rollback]`
```

---

### /test

```markdown
---
name: test
description: Run the [Project Name] test suite. Use before any deployment, after writing new features, or to validate a bug fix. Runs [test types] and reports results.
---

# Test

Runs the test suite for [Project Name].

## Usage
- `/test` — run all tests
- `/test --unit` — unit tests only (faster)
- `/test --e2e` — end-to-end tests only

## Workflow

1. **Install/verify dependencies** — `[e.g., npm install]` if package-lock has changed
2. **Unit + integration tests** — `[e.g., npm run test]`
3. **E2E tests** — `[e.g., npx playwright test]` (requires running dev server or staging)
4. **Report** — summarize pass/fail counts; if failures, show the failing test names and errors
5. **If failures**: suggest likely causes and fixes before asking user what to do

## Project specifics
- Test framework: [e.g., Vitest + Playwright]
- Test directory: [e.g., `src/**/*.test.ts` + `e2e/`]
- Coverage threshold: [e.g., 80% or "none set yet"]
- Known flaky tests: [list any, or "none known"]
```

---

### /review

```markdown
---
name: review
description: Code review for [Project Name]. Checks security, correctness, and quality. Use before merging a feature, before client demos, or when something feels fragile. Focuses on [project's highest-risk areas: e.g., payment logic, auth, data handling].
---

# Code Review

Reviews staged/recent changes in [Project Name] for security, correctness, and quality.

## Workflow

1. **Identify what changed** — `git diff --stat` to see scope
2. **Security pass** — check for: injection vulnerabilities, exposed secrets, auth bypass risks, insecure direct object references, [project-specific risks]
3. **Correctness pass** — does the logic match the intent? edge cases handled?
4. **Quality pass** — readability, test coverage, adherence to [project conventions]
5. **Report** — categorize findings as: 🔴 Must fix, 🟡 Should fix, 🟢 Suggestion
6. **Summary** — overall assessment: ready to merge / needs work

## Project specifics
- High-risk areas to always check: [e.g., "Stripe webhook handling", "user data queries", "auth middleware"]
- Conventions to enforce: [reference CLAUDE.md conventions]
```

---

### /migrate

```markdown
---
name: migrate
description: Run database migrations for [Project Name]. Use after schema changes, before deployment, or when setting up a fresh environment. Runs [ORM] migrations against the target database.
---

# Migrate

Runs pending database migrations for [Project Name].

## Usage
- `/migrate` — run pending migrations against development DB
- `/migrate --staging` — run against staging database
- `/migrate --prod` — run against production (confirms before proceeding)

## Workflow

1. **Show pending migrations** — `[e.g., npx prisma migrate status]`
2. **Review** — list the pending migration files and summarize what they change
3. **Confirm** with user before running against staging or production
4. **Run** — `[e.g., npx prisma migrate deploy]`
5. **Verify** — confirm migration completed successfully; show any warnings
6. **If production**: run a quick sanity check query to verify data integrity

## Project specifics
- ORM: [e.g., Prisma]
- Migration directory: [e.g., `prisma/migrations/`]
- Schema file: [e.g., `prisma/schema.prisma`]
- Dev DB: `[connection string location, e.g., DATABASE_URL in .env]`
```

---

## Domain Skill Template

Use this for project-specific workflows. The name should be a verb phrase.

```markdown
---
name: [skill-name]
description: [What this workflow does and when to use it, specific to this project. E.g., "Process a refund for a customer order in [Project Name]. Use when a customer requests a refund or a dispute is raised."]
---

# [Skill Name]

[1–2 sentence description of what this workflow accomplishes.]

## When to use
- [Specific trigger 1]
- [Specific trigger 2]

## Workflow

1. **[Step 1]** — [what to do and why]
2. **[Step 2]** — [commands or actions, e.g., `[command]`]
3. **[Step 3]** — [verification or confirmation step]
4. **[Step 4]** — [completion and any follow-up]

## Project specifics
- [Relevant API endpoint, DB table, or service]
- [Edge cases or gotchas specific to this project]
- [Links to relevant docs or code if known]
```
